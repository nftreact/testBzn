'use client';

import React from 'react';

import { Flex } from '@/libs/primitives';

const Banner = () => {
  return (
    <Flex
      justify={'center'}
      align={'center'}
      height={'150px'}
      style={{ border: '1px solid #6a6a6a', borderRadius: '8px' }}
    >
      banner
    </Flex>
  );
};

export default Banner;
